node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example
.vercel
.env*.local
# .claude/ 는 skills/ 만 버전관리한다.
# 디렉터리 자체를 무시하면(.claude/) Git이 안으로 들어가지 않아
# 하위 예외(!)가 동작하지 않으므로 .claude/* 로 적어야 한다.
.claude/*
!.claude/skills/
